apiVersion: kctf.dev/v1
kind: Challenge
metadata:
  name: transleet
spec:
  deployed: true
  powDifficultySeconds: 0
  network:
    public: false
    ports:
      - protocol: "HTTPS"
        targetPort: 8080
  healthcheck:
    # TIP: disable the healthcheck during development
    enabled: true
